// Copy this file to config.js and replace with your own TheSportsDB premium API key.
// config.js is gitignored and must never be committed.
window.APP_CONFIG = {
  API_KEY: 'YOUR_THESPORTSDB_API_KEY',
};
